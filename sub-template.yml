parameters:
  number: ''
variables:
  numberfromparam: $[number]
steps:
- script: echo "Hello from subtemplate -- kapcsos - ${{ parameters.number }}, kerek - $(numberfromparam), szogletes - $[numberfromparam]"